$kind: http-request
name: User By Username - (Save the id)
url: https://localhost:7081/api/test/user-by-username?UserName={{target_username}}
method: GET
queryParams:
  UserName: "{{target_username}}"
scripts:
  - type: afterResponse
    code: |-
      const responseJson = pm.response.json();

      const recipientId = responseJson.id;

      pm.collectionVariables.set("destination_user_id",recipientId)
    language: text/javascript
order: 898
