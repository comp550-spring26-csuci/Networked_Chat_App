$kind: websocket-request
url: wss://{{tunnel-id}}-{{port}}.{{cluster-id}}.{{domain}}/chathub?access_token={{access_token}}
queryParams:
  access_token: "{{access_token}}"
messages: ./.resources/ChatHub.resources/messages
order: 2000
